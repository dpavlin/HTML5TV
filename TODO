+ reorg directories www/media into mirror of media from root of checkout
+ slide capture [s] in mplayer
+ reformat time in table as mm:ss
+ move to GraphicsMagick (supports multi-core)
+ display pictures (slides) while editing
+ calculate exact pre-roll timeout for OSD
- render carousel slides as png

